import React from 'react'

export default function mapf() {
    return (
        <div>

        </div>
    )
}
// (props: { navItems: { Icon: JSX.Element, title: string, url: string }[], logoImageUrl: string })
// {props.navItems.map((curr, i) => (
//     <li key={i}> <a href={curr.url}>{curr.title} {curr.Icon} </a></li>
/* /*MAP- פונקצייה שמבצעת מיפוי- היא עוברת על המערך וכל פעם יש את המישהו הנוכחי ואנחנו אומרים 
לה מה תעשה עם הנוכחי
props-פרמטר שמחזיר היררכיה של תצוגת להצגה דרך המתודה רינדר*/ 